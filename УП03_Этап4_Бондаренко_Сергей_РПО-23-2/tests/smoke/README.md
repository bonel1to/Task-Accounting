# Smoke tests

Для статического проекта smoke-проверка выполняется через `scripts\test.bat`.

Проверяется:

- наличие ключевых файлов;
- синтаксис `script.js`;
- наличие основных UI-маркеров в HTML/JS;
- наличие логики `localStorage` и `validateTaskInput`.
