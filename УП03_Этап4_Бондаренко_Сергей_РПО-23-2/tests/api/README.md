# API checks

У проекта нет backend API. Проверки API этапа 4 адаптированы под HTTP-доступность GitHub Pages и статических ресурсов.

Команда:

```bat
scripts\api-test.bat
```

Проверяются:

- главная страница;
- `styles.css`;
- `script.js`;
- ошибочный сценарий для отсутствующего файла.
