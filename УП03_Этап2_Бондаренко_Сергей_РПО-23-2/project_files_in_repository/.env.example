# Task-Accounting is a static HTML/CSS/JavaScript project.
# The project has no real secrets, tokens, passwords, database credentials,
# or required environment variables.

APP_ENV=development
APP_STORAGE=localStorage
APP_DEBUG=false
APP_PORT=8080
